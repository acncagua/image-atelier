最新（2026-09-12 障害復旧）: SwinIRの同一ID登録再開・完成PNG回収・Windows Job Objectによる子孫終了を修正。110件の回帰とモック推論によるWindows強制終了／Uvicorn再起動を確認。実GPUの再測定は今回実施していない。詳細は [SwinIR検証記録](SWINIR.md)。

前回（2026-09-12 SwinIR追加）: ローカルGPUアップスケールを実装。回帰テスト100件とRTX 5090での実推論を確認。詳細は [SwinIR検証記録](SWINIR.md) を参照。以下は過去の記録です。

最新（2026-09-12追補）: 異常系4件を修正し、計74件のテストが成功。今回の結果は [異常系修正記録](ABNORMAL_CASE_FIXES.md) を参照。以下は前回までの記録。

# 現在の状態

2026-09-12: 指示書①〜③を修正。Python49件＋JavaScript12件の回帰テストと本番ビルドが成功。今回の外部画像API呼び出しは0回。

- ジョブIDの衝突拒否、固定入力による受付確認、別条件・同条件の新規登録を実装。
- 入力の事前寸法調整、ローカル編集履歴、タブ別下書き自動保存を実装。
- ワーカーヘルス、一時停止・再開、旧応答の発見、API再送なしの再処理を実装。
- 既存asset30件・履歴19件・設定・APIキーを保持。バックアップとDB整合性を確認。

詳細・手順・残件は [①〜③の修正記録](FIXES_01_03.md) を参照。

---
以下は2026-09-11時点の旧記録です。「未送信作業の自動保存は未実装」等は今回の修正で解消しました。

# 進捗と残件

2026-09-11更新。実API検証と追加実装を実施した。

| 項目 | 状況 |
| --- | --- |
| 新規生成 | Flare、PNG、1024×1024で実API成功 |
| 元画像のみ・顔資料付き編集 | Sunburst、PNG、1136×1408で実API成功 |
| マスク・局所合成 | 実API成功。選択外1,587,320画素の保持を確認 |
| 保存・比較・履歴・復元 | 実画像で確認 |
| API接続確認 | 設定画面から認証とモデル一覧を確認できる |
| 形式・枚数 | PNG/JPEG/WebP、1〜4枚を実装。PNG1枚以外はモックで検証 |
| 不明な処理 | 初回の実編集1件は結果不明。予約額を保持し、自動再送なし |
| 回帰テスト | 21件成功 |

詳細と費用は [実API検証記録](LIVE_TEST_RESULTS.md) を参照。

入力は編集対象を含め8枚まで、1枚20MBまで。事前費用は不明で、応答の内訳が十分な場合だけ事後概算する。結果不明の自動照会・再送、キャッシュ内訳不明時の費用精算、履歴削除・検索は未実装。未送信作業の自動保存とローカル再処理は2026-09-12に実装済み。

SD元絵とブラッシュアップ済み画像を組み合わせた実作品評価、全品質・全モデルの組み合わせ、特殊ICC、実際のSD Extras受け渡しは未検証。顔の同一性と採用可否は利用者の判断が必要。

人物自動対応、顔検出、高度な資料管理、SwinIR内蔵、SD自動転送、ChatGPT会話統合、LAN・クラウド公開は当初の初期版の対象外。
