最新（2026-09-12）: 指示書①〜③の回帰テスト61件が成功。今回の実施内容は [修正記録](FIXES_01_03.md) を参照。以下は旧記録。

更新: 予算精算・既存履歴移行・結果不明の予約保持のテストを追加し、全23件成功。

追記: 実API検証と修正後の最新結果は [実API検証記録](LIVE_TEST_RESULTS.md) を参照。以下は初期モック検証時の記録です。

# 検証記録

実施日: 2026-09-11。実API呼び出しは0回。作品評価は行っていない。

追記: APIキー保存用config.local.jsonへの対応後、ファイル優先・環境変数へのフォールバック・UTF-8 BOM・不正設定での秘密情報非表示の試験を追加。全15件成功、本番ビルド成功。有料APIは呼び出していない。

## 自動テスト13件が成功

```powershell
.\.venv\Scripts\python.exe -m unittest discover -s tests -v
npm.cmd run build
```

Pythonテスト13件がすべて成功し、Viteの本番ビルドも成功した。Windowsのサンドボックスでは一時フォルダーと子プロセス作成が制限されたため、依存導入・テスト・ビルドを許可された通常実行環境で検証した。SD環境は変更していない。

| 対象 | 結果 |
| --- | --- |
| 寸法検証 | 1920×1088、2048×1152、3840×2160を許可。1920×1080などを拒否 |
| 画像の役割・順序 | 編集対象が最初、顔資料・塗り資料の説明が送信順に一致 |
| マスク | 塗った範囲のAPIアルファが0、未選択が255。消しゴムにも対応 |
| 合成 | 全10,000画素を調べ、マスク外のRGBA値が一致。寸法不一致を拒否 |
| 保存 | 日本語・空白パスで保存し、同一名を上書きしない |
| 保存失敗 | 出力先の権限エラーでも内部結果と回収案内を保持 |
| モック編集・新規生成 | コピーと検査用パターンの出力・寸法確認が成功 |
| 重複防止 | 同じジョブIDと実行中の同条件を1ジョブに集約 |
| 通信断・再起動 | 結果不明として保持。自動再送しない。予約額を保持 |
| 待機取消 | API送信前だけ取消でき、再起動前の待機は取消になる |
| APIエラー | 400/401/403/404/429/500を分類。例外本文を記録しない |
| 実API経路 | HTTP通信を完全に置換してmultipart、マスク、応答の契約を検証 |
| 予算と費用 | 予約超過を拒否。十分な内訳だけ概算し、不明な内訳を0扱いしない |
| ローカル保護 | 異なるHost/Origin、トークンなしの変更操作を拒否 |

テストランナーはhttpxを使うStarlette TestClientについて非推奨警告を1件出す。13件は成功しており、アプリの動作エラーではない。

## Windowsとブラウザーで主要操作を確認

Codex内蔵ブラウザーをCUAと組み込みPlaywrightロケーターで操作した。外部Playwrightへの切替はしていない。URLは http://127.0.0.1:18791 、タイトルはImage Atelier。デスクトップ1536×1024と、390×844相当の幅を確認した。

検査用の1920×1088の図形画像をローカルで作成し、元画像と顔資料としてアップロードした。指示文確認、モック編集、結果表示、PNG保存、条件復元、部分修正、等倍表示、ブラシ描画、取り消し・やり直し、画面に合わせる、局所合成を実行した。

ブラウザー座標(540,450)から(640,490)へのドラッグが、実画像の座標(862.5,405.609375)から(962.5,445.609375)として記録された。等倍でのキャンバス位置を引いた座標と一致する。縮小へ戻した後の同マスクを送信し、1920×1088の生出力と局所合成を得た。

start.ps1から再起動し、履歴とマスクの復元を確認した。続けて二重起動を試し、既存プロセスを変更せずに追加起動が拒否された。

| ブラウザー確認 | 結果 |
| --- | --- |
| URL・ページタイトル | 一致 |
| 初期画面 | 空白画面ではなく、入力・比較・履歴を表示 |
| フレームワークエラー | なし |
| コンソール | 関連するerror/warnなし |
| 操作 | モック編集からPNG保存、復元、マスク合成まで成功 |
| 狭い画面 | 1列表示。入力に重なる固定ボタンを廃止し、履歴表を横スクロール化 |

## 見本との比較で確認した点

concept.pngと最新のブラウザー画像をview_imageで開き、配色、配置、文字、操作部品、余白、画像表示を確認した。画面設計を踏襲し、要件に必要な操作を追加した。完全なピクセル一致とは扱っていない。

| 比較点 | 実装・判断 |
| --- | --- |
| 配色 | 白いパネル、冷たい薄灰のキャンバス、青緑の主操作を維持 |
| 配置 | 左の資料・指示、左右比較、その下の履歴を維持 |
| 文字 | 日本語の可読性を優先。モデル名の欄を広げて省略を修正 |
| 枠・余白 | 細い灰色境界線と小さな角丸を統一 |
| 画像 | 入力と結果を切り抜かずに表示し、倍率・移動を連動 |
| 操作 | 寸法入力、比率、マスク、プリセット、保存を要件に合わせて追加 |

見本からの意図的な変更は、GIFを非対応と明記したこと、OSウィンドウボタンを省いたこと、履歴とジョブを統合したこと、拡大縮小を文字ボタンにしたこと。先頭画面の追加文言は要求機能と説明に限定した。未承認の作品画像や架空の実績は追加していない。

画面証跡: ui-desktop.png、ui-mobile.png。スクリーンショットはブラウザーの表示領域やスクロールバーを含むため、画像ファイルの寸法が設定したviewportと完全には一致しない場合がある。

## 残る検証

実APIのモデルアクセス、実画像での顔・質感の維持、最大容量の入力、特殊ICC、実際のSD Extrasへの手渡しは未検証。ブラウザーの全操作を網羅した自動E2Eは未作成。READMEの日本語lintはsudachipy不足で実行できず、スキルの手動チェックリストで通読した。
