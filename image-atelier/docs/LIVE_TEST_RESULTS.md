更新: 成功分の予約額を記録済み概算額で精算する方式へ変更済み。成功4件0.094588 USD＋結果不明1件の予約1 USDとなり、予算5 USDに対する残りは3.905412 USDです。以下の「5 USD予約」は試験終了時点の旧方式の記録です。

# 実APIでの検証結果

2026-09-11。利用者の許可範囲は最大10枚、予算5 USD。指定された人物資料フォルダーの「サリナ_19歳_侍女姿.png」を使用した。

## 主要な生成・編集経路は成功

| 試験 | 結果 | 実寸法 | 時間 | 使用量からの概算 USD |
| --- | --- | --- | --- | --- |
| 元画像のみの編集・初回 | 結果不明 | 未取得 | 0.19秒 | 不明 |
| Flare・参照なし生成 | 成功 | 1024×1024 | 17.46秒 | 0.013515 |
| Sunburst・元画像＋顔資料 | 成功 | 1136×1408 | 31.94秒 | 0.027969 |
| Sunburst・マスク付き継続編集 | 成功 | 1136×1408 | 32.72秒 | 0.027959 |
| Sunburst・元画像のみの比較試験 | 成功 | 1136×1408 | 27.05秒 | 0.025145 |

送信を試みた回数は5回、受け取ったAPI画像は4枚。局所合成1枚はアプリで作成したもので追加課金はない。成功分の概算合計は **0.094588 USD**。初回の結果不明分は含まないため、これを請求総額とは扱わない。請求は公式Usageで照合する必要がある。

各試験で1 USDを予約し、結果不明分を含む5 USDを引き続き確保している。予約額は実際の請求額ではない。現在は設定された5 USDの予算に達しているため、新たな有料送信はアプリ側で拒否される。追加試験は実施しない。

初回は画像・使用量・リクエストIDの取得前に終了した。送信データの組み立てを外部通信なしで再現すると成功し、モデル一覧の取得も成功したが、初回の原因は確定できなかった。自動再送は行っていない。残りの予算内で別ケースを検証した後、比較用の資料なし編集を明示的に1回追加した。

## 参照資料の効果は目視評価が必要

元画像は1122×1402のため、原画像を保存したまま、周囲に透明余白を追加して1136×1408にした。顔資料は同画像から矩形で切り出した。別のSD元絵ではないため、「SDの顔形状だけを戻し、別の塗りを維持する」条件の検証にはなっていない。

資料なし・顔資料付きとも、顔・髪型・衣装・姿勢・背景・文字の主要な特徴は残った。一方、顔の目元や輪郭、服の陰影など細部は変化している。今回の1組だけでは、顔資料が明確に優れるとも、顔の同一性が保証されるとも判断できない。透明余白の見た目も入力と異なり、画像全体が完全保存されるわけではない。

マスク試験では白いエプロンの一部の陰影を調整した。API出力全体には選択外の変化があり得るが、局所合成結果では選択外1,587,320画素のRGBA値を照合し、変化した画素は0だった。境界内のぼかしは通常の画像処理で行っている。

採用可否、顔の印象、肌のつながり、SDでの顔戻しやクリスタ手修正の要否は利用者の目視判断が残る。比較画像はアプリ起動中の [実画像比較](http://127.0.0.1:18791/evaluation) で確認できる。

## 不足と実機で見つけた問題を修正

- 設定画面に、画像を生成しない実API接続確認を追加。
- 実APIを無効化した後の待機ジョブを送信前に取消。
- アップロード用20MB制限をAPI生成結果に誤適用しないよう修正。
- 成功HTTP応答をデコード前に保存し、例外の種類を秘密情報なしで記録。
- 共有SQLite接続の読み取りも排他制御し、画面要求が重なった際のエラーを修正。
- 出力形式をPNG/JPEG/WebP、枚数を1〜4枚から選べるよう追加。予約額は枚数分を確保し、各出力の保存と局所合成に対応。

形式・複数枚の追加機能は外部APIをモックして検証した。今回の有料試験はPNG・各1枚で、JPEG/WebP・複数枚の実API試験は実施していない。新規生成はFlare、編集はSunburstで確認した。全モデル・全品質の組み合わせを網羅した試験ではない。

回帰テストは21件成功。実行中ロックファイルを除く画面コード・ビルド結果・文書・保存データ59ファイルを調べ、APIキーの混入は0件だった。キーの値は表示していない。

## 再検証の方法

通常のテストは `python -m unittest discover -s tests -v` で実行する。外部APIはモックされる。`live_validation.py` は今回の許可済み試験専用で、CIや通常テストからは呼ばない。既に記録したケースは再送せず、試験履歴はdata/live-validation.jsonとアプリの履歴に残している。
